package gov.nist.healthcare.iz.record.generator.field;

public enum FieldType {
    CODED, RANDOM, FIXED, VOCABULARY, DOB, NAME, STATE_AWARE, ENUM, VALUE_DISTRIBUTION, VALUE_LENGTH, MAPPED, DATE_OP, DATE_BETWEEN, COPY, CROSS
}
