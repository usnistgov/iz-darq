import { ChartFilterPipe } from './chart-filter.pipe';

describe('ChartFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChartFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
