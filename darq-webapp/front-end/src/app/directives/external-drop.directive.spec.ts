import { ExternalDropDirective } from './external-drop.directive';

describe('ExternalDropDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalDropDirective();
    expect(directive).toBeTruthy();
  });
});
