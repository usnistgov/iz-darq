import { AgeGroupsPipe } from './age-groups.pipe';

describe('AgeGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
